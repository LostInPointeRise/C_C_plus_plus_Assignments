#include <stddef.h>

namespace compare
{

    /**
     * @brief function to compaare two int values 
     * @param (int) a, first value needed for the comparison
     * @param (int) b, second value needed for the comparison
     * 
     * @return (double) the bigger of the above arguments
    */

    int maxf(int a, int b){};

    /**
     * @brief function to compaare two int values 
     * @param (double) a, first value needed for the comparison
     * @param (double) b, second value needed for the comparison
     * 
     * @return (double) the bigger of the above arguments
    */

    double maxf(double a, double b){};
}